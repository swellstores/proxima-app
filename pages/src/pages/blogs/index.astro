---
import { SwellStorefrontCollection } from '../../swell/api';
import ThemePage from '../../components/ThemePage.astro';
---

<ThemePage
  id="blogs/index"
  title="Blogs"
  getData={async (swell: Swell, theme: SwellTheme) => {
    const blogs = new SwellStorefrontCollection(
      swell,
      `content/blogs`,
      theme.page?.query
    );

    return {
      blogs,
    };
  }}
/>
