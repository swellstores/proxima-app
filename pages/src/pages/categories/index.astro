---
import { Swell, getCategories } from '@swell/storefrontjs';
import ThemePage from '@/components/ThemePage.astro';
---

<ThemePage
  id="categories/index"
  getData={async (swell: Swell) => {
    const categories = await getCategories(swell);

    return {
      categories,
    };
  }}
/>
